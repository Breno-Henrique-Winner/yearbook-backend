info:
  name: Validação
  type: http
  seq: 11

http:
  method: POST
  url: https://legendary-barnacle-v6xgp45vj6rp2xwjg-3000.app.github.dev/mensagens
  body:
    type: json
    data: '{ "autorId": 1 }'
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve retornar status 400 sem texto", function() {
          expect(res.getStatus()).to.equal(400);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
